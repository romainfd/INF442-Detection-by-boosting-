#include <vector>

void displayMatrix(std::vector<std::vector<int> >& matrix) { };
std::vector<std::vector<int> > emptyMatrix(int& l, int& c) { };
std::vector<std::vector<int> > imageIntegrale(std::vector<std::vector<int> >& img) { };
int main(int argc, char** argv) { return 0; };



